#include <fstream>
#include <filesystem>

#include "config.hpp"

// using namespace os2::config;
/*
bool ConfigurationManager::Load(const std::string& fileName) {
  std::filesystem::path filePath("C:\\Users\\mateu\\Desktop\\" + fileName);

  if (!std::filesystem::exists(filePath)) return false;

  std::ifstream file(filePath);

  try {
    file >> this->config_;
  } catch (const std::exception& e) {
    return false;
  }
  return true;
}

*/
